name: Repo checks (placeholder)
on:
  push:
  pull_request:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Info
        run: echo "Template pronto. Nenhuma validação automática configurada."
